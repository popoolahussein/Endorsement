let app = "";

function createApp() {
  app.innerHTML += `Let's create mobile app!`
};

const appSettings = {
  databaseURL: "https://mobile-app-3a7fa-default-rtdb.europe-west1.firebasedatabase.app/"
}
